{
  "name": "@homeauto/heater",
  "version": "1.0.0",
  "description": "A fake heater that listens to and emits mqtt events",
  "author": "Tom Pretty",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "run": "node dist/main.js",
    "start": "nodemon --inspect=0.0.0.0:9222 --nolazy dist/main.js"
  },
  "dependencies": {
    "@homeauto/lib": "1.0.0",
    "mqtt": "^4.2.8"
  }
}
